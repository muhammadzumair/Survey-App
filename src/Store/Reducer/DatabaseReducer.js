import actionTypes from '../actionTypes';

let INITIAL_STATE = {
    
}

export default function dbReducer(state = INITIAL_STATE, action) {
    switch (action.type) {
        

        default:
            return state;
    }
}